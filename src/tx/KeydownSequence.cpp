// Lyra — KeydownSequence.  See KeydownSequence.h.
// Phase 1 empty skeleton — populated in Phase 2.

#include "tx/KeydownSequence.h"

namespace lyra::tx {

KeydownSequence::KeydownSequence() = default;
KeydownSequence::~KeydownSequence() = default;

}  // namespace lyra::tx
