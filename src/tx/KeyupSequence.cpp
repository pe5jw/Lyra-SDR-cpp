// Lyra — KeyupSequence.  See KeyupSequence.h.
// Phase 1 empty skeleton — populated in Phase 2.

#include "tx/KeyupSequence.h"

namespace lyra::tx {

KeyupSequence::KeyupSequence() = default;
KeyupSequence::~KeyupSequence() = default;

}  // namespace lyra::tx
