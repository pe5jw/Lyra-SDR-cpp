// Lyra — WDSP channel lifecycle wrappers (§6 / §10.2 wdsp layer).
//
// Header-only wrappers for OpenChannel / CloseChannel /
// SetChannelState.  OpenChannel(..., block=1) — the previously-wrong
// parameter, now correct.
//
// Phase 1 empty skeleton — populated in Phase 2 per
// docs/TX_ARCHITECTURAL_MAPPING.md §6.

#pragma once

namespace lyra::wdsp {

// Lifecycle wrappers populated in Phase 2.

}  // namespace lyra::wdsp
