// Lyra — iqc cffi declarations (§9 / §10.2 ps layer).
//
// Header-only cffi declarations for create_iqc / destroy_iqc /
// xiqc() / SetTXAiqcRun.  Inert in v0.2 (Rule 10) — present but
// empty; filled in v0.3 PureSignal.
//
// Phase 1 empty skeleton — populated in Phase 2 per
// docs/TX_ARCHITECTURAL_MAPPING.md §9.

#pragma once

namespace lyra::ps {

// cffi declarations populated in Phase 2 / v0.3 PureSignal.

}  // namespace lyra::ps
