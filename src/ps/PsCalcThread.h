// Lyra — PS calc thread (§9 / §10.2 ps layer).
//
// Empty placeholder; v0.3 fills with the calcc semaphore consumer
// loop.  Inert in v0.2 (Rule 10).
//
// Phase 1 empty skeleton — populated in Phase 2 per
// docs/TX_ARCHITECTURAL_MAPPING.md §9.

#pragma once

namespace lyra::ps {

class PsCalcThread {
public:
    PsCalcThread();
    ~PsCalcThread();
};

}  // namespace lyra::ps
