# Thetis vs Lyra-cpp â€” TX & TCI Deviation Audit

**Snapshot:** 2026-06-04 (overnight 3-lens adversarial audit)
**Scope:** TX path + TCI audio. RX excluded.
**Operator directive:** "Do as Thetis does, no variation"
**Known live bug:** Lyra TX via TCI (MSHV FT8) radiates constant tone â†’ zero PSKReporter spots.

## Executive Summary

- **Mic-source dispatch never auto-flips to TCI on `trx:0,true,tci`.** If operator's Prefs.micSource is still Mic1 when MSHV keys via TCI, every audio sample is silently dropped at `tx_dsp_worker.h:208`. Modulator runs against dead air â†’ constant residual tone shaped by the 50 ms cosÂ² MOX fade. **Fix:** wire `TciServer::trxRequested(true)` â†’ `TxDspWorker::setActiveMicSource(MicSource::Tci)` in `main.cpp` (~25 LOC).
- **`kInSize=128` deviates from the operator-locked `getbuffsize()=64` rule at 48 kHz mic.** Three lenses converged. Affects fexchange0 cadence, TCI inbound block floor, CHRONO request math â€” straddles FT8 8-tone symbol boundaries. **Fix:** revert in lockstep â€” `tx_channel.cpp:65` kInSize=64, `tx_dsp_worker.h:97` kBlockSize=64, `tci_server.h:242` kTciTxBlockSamples=64.
- **`TxChannel::Mode` enum is USB/LSB only; DIGU/DIGL silently map to USB/LSB.** Strips digital-mode awareness from the chain â€” no PHROT-off-for-digital gate possible because the mode never reaches WDSP correctly. **Fix:** extend the enum to include DIGU=10/DIGL=9 and route `kTxaModeDIGU/DIGL` from `modeToWdsp()`.
- **MSHV's TCI source-token must be bench-verified before any DSP-side fix.** If MSHV omits the `tci` source token in its `trx` command, `txAudioOwner_` never acquires and binary audio frames drop at `tci_server.cpp:634`. Wireshark capture is the 5-minute test that disambiguates the FT8 root cause stack.
- **Fresh-install TX power deficit stack: Leveler OFF + Leveler decay 500 ms + ALC max_gain=1.0 = cumulative ~10â€“15 dB quieter than reference until QSettings persists.** Push reference defaults explicitly in `TxChannel::open()` init-setters block.

## HIGH-severity deviations

| ID | Title | Thetis | Lyra | Impact | Fix |
|---|---|---|---|---|---|
| MIC-001 / handleTrx-gap | TCI `trx:0,true,tci` does not auto-switch active mic source to TCI | `Console/TCIServer.cs` handleTrx + `cmaster.SetTciSource(true)` â†’ AAmixer routes TCI audio into TXA input (UpdateAAudioMixerStates, ~28217â€“28333) | `src/tci_server.cpp:1258-1290` keys MOX + acquires `txAudioOwner_` but never calls `setActiveMicSource(MicSource::Tci)`; only `Prefs.micSource()` writes it (`main.cpp:613-622`). Drop gate at `src/tx_dsp_worker.h:208` | Prime FT8 root-cause candidate. If operator's Prefs.micSource was Mic1 during the failing bench, all MSHV audio dropped silently â†’ modulator emits cosÂ²-shaped residual = "constant tone" | Subscribe `TciServer::trxRequested(true)` in `main.cpp` lambda â†’ `TxDspWorker::setActiveMicSource(MicSource::Tci)`; restore prior source on `trx:0`. Verify with `TciMicSource::droppedSamples` |
| TX-004 / TCI-002 / WIRE-002 / WIRE-003 / TCI-018 | `kInSize`/`kBlockSize`/`kTciTxBlockSamples`/CHRONO floor = 128, deviates from operator-locked `getbuffsize()=64` @ 48 kHz | `wdsp/channel.c` getbuffsize() rule: in_size = 64 at 48 kHz. `cmaster.c` HL2 path drives fexchange0 at this cadence. `cmaster.cs:1313` `txBlock = GetBuffSize(targetRate) = 64` | `src/tx_channel.cpp:65` `kInSize = 128` (Â§15.29 bump, conflates panadapter FFT size with per-block in_size); `src/tx_dsp_worker.h:97` kBlockSize=128; `src/tci_server.h:242` kTciTxBlockSamples=128; CHRONO floor at `src/tci_server.cpp:786-789` doubled | fexchange0 runs at half reference cadence (~375 Hz vs 750 Hz). MSHV pushes 2400-sample bursts drained in mis-sized chunks; FT8 8-tone symbol (160 ms = 7680 samples) crosses block boundaries at non-reference granularity. Strong FT8 candidate | Revert in lockstep: 3 constants â†’ 64, ring `8*64=512` at `tx_dsp_worker.cpp:54`. Re-validate EP2 packer 64â†’126 accumulation still works |
| TX-009 | `TxChannel::Mode` enum lacks DIGU/DIGL/CW/AM/FM/SAM â€” DIGU silently routes as USB | `wdsp/TXA.c`: TXA_DIGU=10, TXA_DIGL=9 distinct; mode propagated so downstream stages (PHROT, EQ, compressor) gate on digital | `src/tx_channel.h:58` `enum class Mode { USB, LSB }`. `modeToWdsp()` returns only kTxaModeUSB/LSB. DIGU from TCI lands as USB; no digital-mode-specific bypass possible | DIRECT FT8 SYMPTOM PATH: digital modes traverse SSB-voice processing chain. Even with PHROT currently dormant (TX-018), the missing mode dispatch blocks any future per-mode bypass logic. Combined with kInSize=128 = compound FT8 failure | Extend enum: USB/LSB/DIGU=10/DIGL=9/CWU/CWL/FM/AM/SAM/DSB. Wire `modeToWdsp()` to return real TXA_* ints. Gate PHROT OFF for DIGU/DIGL/DRM. Plumb TCI's DIGU through |
| TCI-009 / MSHV-handshake | MSHV TCI source-token bench-verification gap | `Console/TCIServer.cs:5535-5557` `TryAcquireActiveTxAudioListener` requires client to send `tci` source token in trx command for ownership | `src/tci_server.cpp:634` same owner gate; `:1258-1290` "always keys MOX" branch even without token; `:628` rate-limited diag of owner status but no specific log for "0 audio frames received this keydown" | If MSHV's TCI v1.x client sends `trx:0,true` without source token (or sends `mic` instead of `tci`), MOX engages, `txAudioOwner_` never assigned, all TX_AUDIO_STREAM frames drop at the owner gate â†’ constant carrier symptom | Wireshark MSHV port 50001 trx command. Add rate-limited "TCI keydown but 0 audio frames in N ms" log at keyup. If MSHV omits token, accept fallback or document operator-prep requirement |
| FRESH-BOOT-STACK (TX-001 + TX-005 + TX-006 + ALC-001) | Compounding fresh-install TX power deficit (~10â€“15 dB) | `Console/radio.cs:3017-3018` Leveler ON; `:2998-2999` Leveler decay 100 ms; `:2960` ALC decay 10 ms; UI defaults ALC max_gain LINEAR 3.0, Leveler max_gain LINEAR 15.0 | `src/tx_channel.cpp` open() pushes NONE â€” relies on WDSP create-time defaults (Leveler run=0, max_gain=1.0, decay=500 ms; ALC max_gain=1.0). QSettings autoload eventually fills gap but fresh install is quiet | Operator's bench tests done before QSettings persistence see TX ~10â€“15 dB quieter than reference. Task #42 "6 dB gap" likely partially open even after Â§15.27 â€” that fix addressed ALC units only, not Leveler ON/decay/headroom | Init-setters block in `open()`: `SetTXALevelerSt(1)` + `SetTXALevelerTop(15.0)` + `SetTXALevelerDecay(100)` + `SetTXAALCMaxGain(3.0)` + `SetTXAALCDecay(10)` + `SetTXAALCSt(1)`. ~10 LOC |

## MED-severity deviations

| ID | Title | Thetis | Lyra | Impact | Fix |
|---|---|---|---|---|---|
| TX-002 + TX-018 (merged) | PHROT docstring-vs-code divergence (latent FT8 trap) | `wdsp/TXA.c:71-78` create_phrot(run=0). `setup.cs:18749` operator-driven (default unchecked) | `src/tx_channel.h:30-31` claims "PHROT on at open"; `src/tx_channel.cpp` open() never calls `SetTXAPHROTRun(1)`. Reality: WDSP default OFF wins. Docstring lies | Currently dormant by accident. If a future contributor "fixes" the docstring by adding the setter call, FT8 phase coherence detonates. Auditors mislead | Rewrite docstring to match code (PHROT defaults OFF). Add Task #109 PHROT toggle with hard digital-mode-OFF gate (DIGU/DIGL/DRM) before enabling for SSB |
| TX-003 | TXA dsp_rate=96k with mic/out 48k forces resampler stages reference doesn't run | HL2 path = 48/48/48 single-rate; `wdsp/TXA.c` rsmpin/rsmpout run=0 when in==dsp | `src/tx_channel.cpp:148-154` open() pins (48000, 96000, 48000). rsmpin.run=1 + rsmpout.run=1 â€” two extra resamplers around SSB chain | Extra ~10â€“20 sample group delay, ~2Ã— TX hot-path CPU; downstream dsp_size constants doubled. Compounds with kInSize=128 | Default open() to (48000, 48000, 48000); resamplers become no-ops |
| TX-008 | Bandpass set BEFORE mode (reference: mode then bandpass) | `Console/radio.cs:2692/2738` SetTXAMode then SetTXABandpassFreqs; `wdsp/TXA.c:33â†’239` mode set before bandpass create | `src/tx_channel.cpp:102-132` pushBandpassLocked always pushes `SetTXABandpassFreqs` then `SetTXAMode` | One-block stale-bp0 window on rate change or mode-without-bandpass paths. Benign in steady state | Reverse the two calls; SetTXAMode first |
| TX-016 | AM/FM/CW/SAM silent fallthrough to LSB in setMode | `wdsp/TXA.c` full mode set | `src/tx_channel.h:58` enum has only USB/LSB; non-{USB,LSB} silently uses LSB integer 0 | Operator picks AM â†’ wrong modulator runs. Â§15.23-class trap | Add explicit `qWarning()` + early-return guard in setMode for non-{USB,LSB} until v0.2.2 lands |
| TX-006 | ALC max_gain not pushed at open(); fresh install gets WDSP default 1.0 | `Console/setup.cs:9299` operator UI typical 3.0 LINEAR | `src/tx_channel.cpp` open() does not call `SetTXAALCMaxGain`. First-launch operator gets max_gain=1.0 = no ALC headroom = quiet TX until QSettings | First-launch FT8 bench likely ran with wrong ceiling | Push 3.0 explicitly in open() init-setters (covered by FRESH-BOOT-STACK fix) |
| TX-001 / TX-005 | Leveler default OFF and decay 500 ms (reference ON, decay 100 ms) | `Console/radio.cs:3017-3018` Leveler ON, top=15.0, decay=100 ms | open() never pushes leveler init-setters â†’ WDSP create-defaults (run=0, decay=500 ms) | ~3â€“6 dB lower average SSB voice power; sluggish recovery. Not FT8 root cause (FT8 is constant-envelope) but a real reference-parity miss | Covered by FRESH-BOOT-STACK fix |
| TCI-003 | TUN keys MOX instead of separate TUN flag (no CW-style tune tone) | `Console/TCIServer.cs:4343-4364` handleTune sets independent TUN state | `src/tci_server.cpp:1192-1249` TUNE branch calls `stream_->requestMoxFromTci(true)`; comment locks in voice-modulated tune | MSHV TUNE button â†’ silent carrier in dummy load. Task #74 already pending | Land separate tune-carrier generator path with TUN drive % prefs |
| TCI-017 | Inbound TCI drain timer 10 ms cadence vs reference per-fexchange0 callback drain | `Console/cmaster.cs:1774-1812` drains INSIDE fexchange0 input callback at natural ~750 Hz | `src/tci_mic_source.cpp:74-97` QTimer 10 ms (100 Hz). With kInSize=128 fexchange0 runs ~375 Hz â†’ 3.75Ã— cadence mismatch | Variable inter-block delay â†’ sub-symbol phase drift across FT8 tone boundaries. Secondary FT8 candidate paired with kInSize fix | Wire drain through fexchange0 input request callback (Option A faithful), or reduce timer to 2â€“3 ms (Option B cheap) |
| MOX-001 | 50 ms cosÂ² MOX-edge envelope at EP2 packer applied additionally to WDSP TXA channel-state ramp | `networkproto1.c:1241-1259` packs TX I/Q without envelope; keyup ramp in TXA SetChannelState blocking flush only | `src/mox_edge_fade.cpp` + `src/hl2_stream.cpp:2626` multiplies cosÂ² into TUN AND SSB I/Q each sample. Double-fade | For SSB voice benign once at 1.0. For FT8 keying: first/last 50 ms of every burst has AM modulation. Combined with MIC-001 dropped-audio, the fade tail IS what the demodulator sees â†’ "constant tone" mechanism | Gate `fade_.advance()` on `activeMicSource_ == Mic1`; bypass for TCI source. Or remove EP2-side fade entirely; rely on TXA channel-state blocking-flush like reference |
| ATT-on-TX-port | Â§15.26 PART C `m_bATTonTX` port from Lyra-py to Lyra-cpp not confirmed in audit sources | `console.cs:30293-30327` forces step-att 31 during TX to protect RX ADC | Audit could not confirm port to lyra-cpp; if missing, RX-ADC overloads during TX bench â†’ AGC pumping artifacts mis-attributed to FT8 modem | Inconsistent A/B comparisons during FT8 bench. Not FT8 root cause but pollutes diagnosis | Audit `src/tx_channel.cpp` open()/start() and step-att composer; if missing, port the policy and add to keydown sequence |
| Auto-LNA-during-MOX | Auto-LNA freeze during MOX-edge re-audit (2026-05-18 "volume burst" recurrence) | Reference freezes LNA on MOX edge | Â§15.26 cb58bcb noted Auto-LNA freeze in TX but operator-reported volume bursts have history of state-leak root causes | Operator-perceived AGC surges may misdirect FT8 diagnosis | Unit test asserting no LNA register writes between MOX-keydown and MOX-keyup |

## LOW-severity deviations

| ID | Title | Thetis | Lyra | Impact | Fix |
|---|---|---|---|---|---|
| TX-007 | ALC decay not explicitly pushed; defaults happen to match (10 ms) | `radio.cs:2960` `tx_alc_decay = 10` = WDSP default 0.010 | open() relies on WDSP default | None today; DLL-update risk | Push `SetTXAALCDecay(10)` defensively |
| TX-010 | `SetTXABandpassWindow` operator knob missing | `Console/dsp.cs:221` exposed | Not called; relies on WDSP default wintype=1 (matches) | UI cosmetic only | Defer to Task #49 TX Profile Manager |
| TX-011 | PHROT corner/nstages defaults match reference but not pushed | Driven from operator UI; defaults 338 Hz/8 | WDSP create-defaults inherited | None | Push explicitly only after TX-002+TX-018 PHROT-toggle fix |
| TX-012 | Bandpass sign convention for LSB | `Console/radio.cs` SetTXFilter signed | `src/tx_channel.cpp:90-100` signedEdges() USBâ†’(low,high), LSBâ†’(-high,-low) | None â€” correct | No change. Verify DIGU/DIGL sign in unit test when TX-009 lands |
| TX-013 | TxDspWorker spins during RX-only (~1â€“3% CPU) | TXA pumps only when state==1 | `src/tx_dsp_worker.cpp:198-285` workerLoop runs continuously, skipCount++ when stopped | CPU only; no signal correctness | Documented design; no fix |
| TX-014 | TX channel state starts STOPPED (matches reference) | `radio.cs` create_xmtr state=0 | open() sets state=0, start() from FSM | None â€” correct | No change |
| TX-015 | I=Q=mic packing with patchpanel inselect=2 (matches reference) | `cmaster.cs:5344-5351` I=Q=mono | `src/tx_channel.cpp:436-450` I=Q=mic | None â€” correct | No change |
| TX-017 | `SetTXAPanelRun(1)` not explicit; WDSP default run=1 (matches) | Default run=1 | Not pushed | None | No change |
| TX-019 | `SetTXAALCSt(1)` not explicit; WDSP default run=1 (matches) | Default run=1 | Not pushed | None | Push defensively (covered by FRESH-BOOT-STACK) |
| TCI-001 | TX-in stereo hardcoded LEFT-only | `cmaster.cs:1397-1418` operator switch Left/Right/Both default mix | `src/tci_server.cpp:138-151` always L only | Dormant for MSHV/WSJT/JTDX (mono). Stereo clients lose R + 50% amplitude | Add Prefs `TciTxStereoInputMode`, default Both |
| TCI-004 | CWU/CWL collapse to `CW` on outbound TCI | Reference advertises sideband-distinct | `src/tci_server.cpp:972-977` collapses to `CW` | Logger/contest clients lose CW sideband | Emit `CWU`/`CWL` distinctly; keep cwluBecomesCw as legacy alias only |
| TCI-005 | FM â†’ `NFM` token mapping mismatch | Reference lists both `nfm` and `fm` | `src/tci_server.cpp:975` collapses FMâ†’NFM | Cosmetic; no FT8 impact | Emit `NFM` canonical; accept both inbound |
| TCI-006 | modulations_list appends `,CW` ambiguous alias by default | Reference: no generic `CW` alias | `src/tci_server.cpp:872-876` with cwluBecomesCw_=true | Legacy clients ambiguity | Default `cwluBecomesCw_` to FALSE |
| TCI-007 | RX2/SPLIT VFOBTX route to TXA not plumbed (out of scope; Task #96+) | `cmaster.cs:1266` per-receiver | Not built | Forward-compat | Carry receiver index in CHRONO state when RX2 lands |
| TCI-008 | TX-in resampler failure silent block drop with warn-once | `cmaster.cs:1431-1473` returns empty | `src/tci_server.cpp:688-700` warned-once | Diagnostic | Replace with rate-limited periodic counter |
| TCI-010 | TX-in gain applied AFTER Â±4.0 clamp (by-design overdrive) | Reference: no operator TX-in gain | `src/tci_server.cpp:721-729` gain after sanitize | ALC catches it; by-design | Document or move gain before clamp (operator decision) |
| TCI-011 | CHRONO fallback request samples 480 (matches reference) | `cmaster.cs:1295-1297` fallback 480 | `src/tci_server.h:247` kChronoFallbackRequestSamples=480 | None | No action |
| TCI-012 | RX-out `kAudioPendingCap = 4Ã— packet` defensive drop-oldest | Reference: unbounded with engine-tap-disable on no-sub | `src/tci_server.h:204` 8192 samples | Connect/disconnect transient only | Verify `recomputeStreaming` disables tap promptly |
| TCI-013 / 014 / 015 | sendInit hardcodes `audio_stream_sample_type:float32`, `channels:2`, `tx_stream_audio_buffering:50` | Reflects operator-configurable values | `src/tci_server.cpp:913-917` hardcoded | Cosmetic; matches MSHV defaults | Expose as Prefs; emit operator-selected in sendInit |
| TCI-016 | RX-out duplicates mono â†’ L=R (RX2 not built yet) | True stereo from engine | `src/tci_server.cpp:172-195` dup mono | Out of scope until Task #96 | Route RX1â†’L, RX2â†’R when RX2 lands |
| TCI-019 | TXA OpenChannel `block=1` (verify against reference) | TBD bench-verify | `src/tx_channel.h:16` block=1 | None expected | Read `Console/radio.cs` TXA open to confirm |
| TCI-020 | Double-floor `bufferingMs >= 50` at receive + use | Single-floor at use site | `src/tci_server.cpp:1186` + CHRONO formula | None â€” matched effective behaviour | No action |
| TUN-001 | Explicit TUN-clear on MOX-off edge (stricter than reference) | EP2 `if (!XmitBit) memset` only | `src/hl2_stream.cpp:399-400` explicit clear + emitTone gate | Positive (stricter); no surprises | No change |
| MOX-002 | TrSequencing 10/20 ms keyup matches reference (Â§15.25/Â§15.26) | `console.cs:30350-30384` keyup sequence | Matches | None â€” correct | No change |
| BW-001 | ALC linear-vs-dB (Â§15.27) + per-mode passband sign (Â§15.23) reference-correct | Mapped | `src/tx_channel.cpp:347-366` linear; `:118` signed edges | None â€” correct | No change |
| TXFREQ-001 | TX freq mirrors RX1 at construction (Â§15.26 PART B) | `networkproto1.c:974-1044` HL2 path | `src/hl2_stream.cpp:228-233` + atomic 0x02/0x08/0x0a | None â€” correct | No change |
| HL2-001 | TX step-att `31 - db` encoding both register slots | `console.cs:10657`, `networkproto1.c:1019` | `src/hl2_stream.cpp:2297-2298`, `:2350-2360` | None â€” correct (HIGH-class encoding rule, shipped right) | No change |
| HL2-002 | PA-enable C2 bit 3 (0x08) active-high frame-10 | HL2 wiki / pihpsdr / Quisk cross-ref | `src/hl2_stream.cpp:2329-2340` exact match | None â€” first-RF achieved | No change |
| HL2-003 | Drive level register C1 from txDriveLevel_ (0..255) | `networkproto1.c:1078` | `src/hl2_stream.cpp:2329-2330` | None â€” correct (D-1 commit 8813a5d) | No change |
| HL2-004 | Frame-0 C4 = 0x1C (nddc=4 \| duplex) | `networkproto1.c:967` | `src/hl2_stream.cpp:2258` | None â€” correct | No change |
| SPEC-001 | TX panadapter in-WDSP sip1 tap (pre-iqc) reference-faithful | `cmaster.cs:539-540` `TXASetSipMode/Display` | `src/tx_dsp_worker.cpp:362-374` | None â€” PS-correct for v0.3 | No change (forward-compat: re-audit race window when PS thread lands) |
| THR-001 | Lyra 1-pair semaphore vs reference 2-handle wait (architecturally consistent, no host LR audio path) | `networkproto1.c:1220` `WaitForMultipleObjects(2,...)` | `src/tx_dsp_worker.cpp:294-380` | None | Re-audit when v0.3 PureSignal adds second EP2 path |
| WIRE-001 | EP2 TX I/Q symmetric round + 4-condition zero-fill (STRICTER than reference; positive) | 1-condition zero-fill | `src/hl2_stream.cpp:2644-2663` 4-condition gate | Positive safety deviation | Keep; reference-conformant example |
| Â§15.26 R2 | TX/TUN DSP pump coupling to AK4951 mic clock | Reference TXA pumped at wire cadence unconditionally | Lyra-cpp may inherit Lyra-py defect where empty mic slot starves TXA chain. _TUNE_TONE_MAG=0.5 root-causing 2026-05-17 closed TUN side; non-TUN architecture concern remains | TUN power deficit closed; non-TUN risk theoretical | Verify Â§15.26 R2 fix b68886d ported to lyra-cpp; if not, decouple TxChannel pump from mic path |

## Refuted findings (audit trail)

- **MIC-001 (original framing of "TCI mic source dispatch broken from end to end"):** TCI-DSP lens partially refuted by citing `src/tci_server.cpp:1307-1313` where `prefs_->setMicSource('tci')` IS called when the trx command's source token equals `tci`. Wire lens's original "no auto-flip at all" framing is wrong. The REAL residual gap (captured above as the merged MIC-001/handleTrx-gap HIGH item): the auto-flip only fires when MSHV sends the explicit `tci` source token. If MSHV sends bare `trx:0,true` (or `mic` as source), Prefs.setMicSource never fires and the consumer-side drop at `tx_dsp_worker.h:208` activates. Net effect: MIC-001 stays HIGH but the symptom is gated by MSHV's TCI dialect (bench-verify required, per TCI-009).

No other Round-1 findings were fully refuted. All others were AGREE or EXTEND.

## Methodology + caveats

Three independent lenses (TX DSP, TCI Audio, Wire/Spectrum) each reviewed the lyra-cpp tree against the Thetis reference and produced Round-1 raw findings with file:line citations on both sides. Round 2 was adversarial cross-validation: each lens reviewed the other two's findings, voting AGREE / DISAGREE / EXTEND with source citations. Reconciliation applied: drop on cited refutation, keep on consensus, take the EXTEND version, promote any "missed" item observed by â‰¥2 lenses.

**Caveats:**
- Sources cited but not independently re-verified during reconciliation include: `Console/radio.cs:30350-30384` keyup sequence, `Console/cmaster.cs:1774-1812` OnTCITxAudioInSamples drain, MSHV's actual on-the-wire TCI trx-command source-token (REQUIRES Wireshark bench).
- Â§15.26 R2 fix (TX/TUN DSP pump decoupling from AK4951 mic clock) was applied in the Lyra-py tree per CLAUDE.md; whether the equivalent fix exists in lyra-cpp could not be confirmed from cited sources in this audit.
- ATT-on-TX policy (Â§15.26 PART C) port from Lyra-py to lyra-cpp not confirmed from sources cited.
- Prefs.cpp default values (ALC=3.0 / Leveler=15.0 LINEAR, post-Â§15.27) not directly grep'd during reconciliation; ALC-001 EXTEND notes this needs verification.
- Round 1 found 41 distinct items across the three lenses; Round 2 cross-validated all 41 and surfaced ~13 additional missed/cross-cutting items. Final reconciled list above is grouped by severity, not by Round-1 lens of origin.

## Recommended next move

**Bench-verify MSHV's TCI handshake FIRST, before touching any code.** Capture a single MSHV FT8 TX cycle via Wireshark on port 50001. Look at the exact bytes of the `trx` command and confirm whether the third argument is `tci`, `mic`, or omitted. If `tci` is missing, MIC-001/handleTrx-gap is decisively the root cause: `txAudioOwner_` never acquires, the mic-source auto-flip at `src/tci_server.cpp:1307-1313` never fires, and every TX_AUDIO_STREAM frame drops at the `:634` owner gate â€” the operator hears a constant cosÂ²-shaped carrier with no modulation. The 5-minute Wireshark capture disambiguates HIGH-item #1 (mic-source/handshake) from HIGH-item #2 (kInSize=128) and tells you which to fix first.

**Smallest revertable test:** if MSHV sends the token correctly, revert `kInSize` 128â†’64 in lockstep across `tx_channel.cpp:65`, `tx_dsp_worker.h:97`, `tci_server.h:242`, and the ring sizing at `tx_dsp_worker.cpp:54`. Single commit, 4 constants, fully revertable. Run an MSHV FT8 cycle into a dummy load and check PSKReporter for spots. If FT8 decodes, the Â§15.29 deviation is the bug. If it still fails after both fixes, TX-009 (DIGU not in Mode enum) is the next candidate â€” that's a larger ~25 LOC change but mechanically straightforward. Do not touch PHROT or Leveler init-setters until FT8 is back; those are real reference-parity defects but cannot be the FT8 root cause (PHROT dormant per TX-018, Leveler-off REDUCES dynamic-range manipulation rather than warping it).

